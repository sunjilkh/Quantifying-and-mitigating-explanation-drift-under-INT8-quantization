# =====================================================================
# Quant-XAI - reproducibility requirements
#
# Environment as recorded by CELL 0 of the deposited notebook for the
# run that produced every table and figure in the manuscript:
#   Kaggle Notebooks, 1x NVIDIA Tesla T4 (sm_75), CUDA 12.8
#
# This lists the DIRECT imports of the pipeline only. Transitive
# dependencies are resolved by pip and are deliberately not pinned.
# =====================================================================

# --- deep learning ---------------------------------------------------
# torch was built for CUDA 12.8. Install it from the PyTorch index:
#   pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.10.0
timm==1.0.26

# --- numerics / data -------------------------------------------------
numpy==2.0.2
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.6.1

# --- imaging ---------------------------------------------------------
Pillow==11.3.0
scikit-image==0.25.2
pydicom==3.0.2

# --- quantized inference (ONNX Runtime INT8; Tables N1, N3) ----------
onnx==1.22.0
onnxruntime==1.28.0

# --- figures ---------------------------------------------------------
matplotlib==3.10.0
seaborn==0.13.2

# --- utility ---------------------------------------------------------
psutil==5.9.5

# ---------------------------------------------------------------------
# Notes
# * kaggle_secrets is a Kaggle platform module, not a PyPI package. The
#   pipeline degrades gracefully when it is unavailable.
# * nvidia-* CUDA runtime wheels are installed automatically as torch
#   dependencies and must not be pinned separately.
# * There is no captum / lime / pytorch-grad-cam dependency: Grad-CAM,
#   Grad-CAM++, Integrated Gradients and LIME are implemented natively
#   in CELL 8a of the notebook.
# ---------------------------------------------------------------------
